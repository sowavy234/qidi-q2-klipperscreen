# Read-only validation snippet. Include temporarily from printer.cfg, restart
# Klipper, inspect the log, then remove it. It adds no motion or extrusion.
[gcode_macro Q2_KLIPPERSCREEN_CHECK]
description: Validate that a harmless QIDI screen check macro loads
gcode:
  G4 P0
